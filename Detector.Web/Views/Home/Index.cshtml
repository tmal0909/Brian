@model IEnumerable<IntrudeRecordEntity>

<div class="text-center">
    <h4 class="mb-4">Intrude Record</h4>
</div>

<div>
    <table class="table table-dark small">
        <thead>
            <tr>
                <th>#</th>
                <th>Time (UTC)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.IntrudeTime.ToUniversalTime()</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts{
    <script>
        init(@ViewBag.MaxId);
    </script>
}